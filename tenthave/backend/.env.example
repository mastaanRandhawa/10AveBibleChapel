# PostgreSQL (local)
# DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/tenthave?schema=public"

# PostgreSQL (hosted – e.g. Neon, Supabase, Railway; use SSL if required)
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require"

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tenthave"
PORT=5000
NODE_ENV=development
JWT_SECRET="change-me"
